<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="CustomAppTheme_0" parent="AppTheme">
        <item name="android:background">@android:color/white</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorBackground">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/white</item>
    </style>

    <!-- Base application theme. -->
    <style name="CustomAppTheme_1" parent="AppTheme">
        <item name="android:background">@color/colorDark</item>
        <item name="android:windowBackground">@color/colorDark</item>
        <item name="android:colorBackground">@color/colorDark</item>
        <item name="colorPrimaryDark">@color/colorDark</item>
    </style>

    <!-- Base application theme. -->
    <style name="CustomAppTheme_2" parent="AppTheme">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
    </style>


    <!-- Base application theme. -->
    <style name="CustomAppTheme_3" parent="AppTheme">
        <item name="android:background">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorAccent</item>
        <item name="android:colorBackground">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
    </style>

</resources>
